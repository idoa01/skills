# idoa01-skills

This repo provides Claude Code and Cursor skills for my personal use.

Skills live in `plugins/<name>/commands/<cmd>.md` — that is the canonical source of truth for each skill. Each plugin directory may include a `docs/` subdirectory with supporting reference docs.

See `docs/writing-skills.md` for the contributor guide.
